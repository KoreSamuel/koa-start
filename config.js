const config = {
    port    : 3001,
    database: {
        DATABASE: 'koa_start',
        USERNAME: 'root',
        PASSWORD: 'xiaojie',
        PORT    : 3306,
        HOST    : 'localhost'
    }
};

module.exports = config;
