const config = {
    port    : 3002,
    database: {
        DATABASE: 'koa_start',
        USERNAME: 'root',
        PASSWORD: 'xiaojie',
        PORT    : 3306,
        HOST    : 'localhost'
    }
};

module.exports = config;
